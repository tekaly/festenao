import 'package:tekaly_file_download/download_image.dart';

export 'package:tekaly_file_download/download_image.dart';

Future<void> appDownloadImage(DownloadImageInfo imageInfo) async {
  return await downloadImage(imageInfo);
}
